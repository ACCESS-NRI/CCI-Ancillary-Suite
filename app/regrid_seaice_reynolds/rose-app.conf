[command]
# Regrid the sea ice
default = python ancil_general_regrid.py ${SEAICE_SOURCE} \
	    = --target-lsm=${TARGET_OCEAN} \
	    = -o=${OUTPUT} --ants-config=${ANTS_CONFIG}

[env]
# Source for the sea ice ancillaries- Calendar is set in rose-suite.conf
SEAICE_SOURCE=${ANCIL_MASTER}/atmos/master/seaice/reynolds/198109_201912_${CALENDAR}/v1/reynolds_seaice.nc

# Where to write the output to
OUTPUT=${ANCIL_TARGET_PATH}/ocean/qrclim.seaice

# App configuration
ANTS_CONFIG=${PWD}/rose-app-run.conf

[ants_decomposition]
x_split=10
y_split=10

[ants_regridding_horizontal]
scheme=Linear
