[command]
default = python landseamask_from_land_fractions.py --land-fractions=${LAND_FRACTIONS} \
        = --output=${OUTPUT}

[env]
OUTPUT=${ANCIL_TARGET_PATH}/land_sea_mask

[file:${ANCIL_TARGET_PATH}/land_sea_mask]
mode=mkdir
